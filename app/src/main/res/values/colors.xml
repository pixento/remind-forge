<?xml version="1.0" encoding="utf-8"?>
<resources>

    <!--
        Must stay equal to NeutralLightBackground in ui/theme/Color.kt (values-night to
        NeutralDarkBackground). The platform window is styled from XML and the page from Compose,
        so the two can't share a constant - keep them in step by hand.
    -->
    <color name="window_background">#F2F2F6</color>
</resources>
